export enum Toggle {
  ENABLE = ' Enable ',
  DISABLE = ' Disable ',
}

export enum Switch {
  ON = ' ON ',
  OFF = ' OFF ',
}

export enum UserRole {
  USER = 'User',
  ADMIN = 'Admin',
  OWNER = 'Owner',
}
